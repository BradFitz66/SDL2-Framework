{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "cppdbg",
            "request": "launch",
            "cwd": "${workspaceRoot}/build/",
            "preLaunchTask": "build",
            "program": "${workspaceRoot}/build/SDL2_Test.exe",
            //Use externalConsole to see output
            "externalConsole": true
        }
    ]
}